GET     /journal        저널을 모두 가져옵니다.
GET     /journal/id     특정 저널과 댓글을 가져옵니다.
GET     /category       카테고리를 모두 가져옵니다.
GET     /category/id    특정 카테고리를 가져옵니다.

PUT     /category/name  카테고리를 생성합니다.
PUT     /journal/name   저널을 작성합니다.
required: body.content

PUT     /comment/id     코멘트를 작성합니다. id 는 저널 아이디를 기입합니다.
required: body.comment

DELETE  /category/id    카테고리를 삭제합니다.
DELETE  /journal/id     저널을 삭제합니다.